[package]
name = "eyd"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
serde_json = "1.0"
mountpoints = "0.2"

[dev-dependencies]
tempdir = "0.3"

[profile.release]
opt-level = "z"
panic = "abort"
strip = true
