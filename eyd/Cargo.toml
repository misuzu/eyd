[package]
name = "eyd"
version = "0.3.0"
edition = "2024"

[dependencies]
fs-more = { version = "0.8.1", default-features = false }
mountpoints = "0.2"

[dev-dependencies]
tempfile = "3.27.0"

[profile.release]
opt-level = "z"
panic = "abort"
strip = true
